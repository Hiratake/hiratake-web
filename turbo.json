{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/*", "lib/*", ".output/*", ".nuxt/*"]
    },
    "lint": {},
    "lint:fix": {},
    "typecheck": {},
    "test": {}
  }
}
