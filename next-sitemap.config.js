/** @type {import('next-sitemap').IConfig} */

const config = {
  siteUrl: 'https://hiratake.dev',
  generateRobotsTxt: true,
  sitemapSize: 7000,
  changefreq: 'yearly',
  autoLastmod: false,
}

module.exports = config
